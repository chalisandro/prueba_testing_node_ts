const{ PUBLIC, TMP } = process.env
console.table(PUBLIC);

const {SHELL, HOMEBREW,npm_lefecycle_script} = process.env
export const character = ['Flash', 'Superman', 'Green Lantern', 'Batman'];
const [, , ,Batman] = character;